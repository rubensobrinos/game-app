{
  "name": "aseso-game-app",
  "version": "0.1.0",
  "private": true,
  "description": "Browser-quizgame + multiplayer-laag (play.aseso.nl). Spec: docs/multiplayer/",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test 'server/**/*.test.*' 'client/**/*.test.*' 'shared/**/*.test.*' 'frontend/**/*.test.*' 'tests/**/*.test.*'",
    "start": "node server/index.mjs"
  },
  "dependencies": {
    "fastify": "^5.11.0",
    "pg": "^8.22.0",
    "redis": "^6.2.0",
    "socket.io": "^4.8.3"
  }
}
