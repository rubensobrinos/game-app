# Startprompt agent 1 — ronde 3

Je werkt in de repo `game-app` (Rounda) als **agent 1** van ronde 3.

STAP 0, VERPLICHT: `ln -s /Users/ruben/game-app/node_modules node_modules`
(staat in .gitignore, ontbreekt dus in deze worktree). Controleer dat
`npm test` draait vóór je iets wijzigt.

Lees dan `docs/agent-opdrachten/ronde-3/README.md` en daarna
`docs/agent-opdrachten/ronde-3/agent-1/BRIEFING.md`.

Doe **alleen ronde 1** uit je briefing. Stop daarna en lever op: de commits,
`npm test`, en per punt één zin over wat je deed. **Niet pushen.**

Nederlands. `devkit check-autonomy --staged` vóór elke commit (max 15
bestanden). Kom je iets tegen dat van een andere agent is: melden, niet bouwen.
